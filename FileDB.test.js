//keep inputing value and check if the value in the json file is correct or not, delete the fileDB after the test
const Store = require('./FileDB.js');

test('Checking create function', () => {
  expect(sum(1, 2)).toBe(3);
});
test('Checking read function', () => {
    expect(sum(1, 2)).toBe(3);
});
test('Checking delete function', () => {
    expect(sum(1, 2)).toBe(3);
});